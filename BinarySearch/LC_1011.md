
## code
https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/submissions/1782938422

```

class Solution {
    public int shipWithinDays(int[] weights, int days) {
        int l=0;
        int h=0;
        for(int i=0;i<weights.length;i++)
        {
            h=h+weights[i];
            l=Math.max(l,weights[i]);
        }
        

        while(l<h)
        {   int mid=l+(h-l)/2;
            if(canship(weights,days,mid))
            {
                h=mid;
            }
            else 
            {
                l=mid+1;
            }

        }

        return l;
        
    }
    public Boolean canship(int[] arr, int days,int  mid)
    {
        int daysneeded=1;
        int current=0;
        for(int i=0;i<arr.length;i++)
        {
            if(current+arr[i]>mid)
            {
                daysneeded++;
                current=0;
            }
            current=current+arr[i];
        }
        if(daysneeded>days)
            {
                return false;
            }
            else 
            {
               return  true;
            }


        

    }
}